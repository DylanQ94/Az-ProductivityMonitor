# Azure Data Factory Artifacts

This folder is reserved for Azure Data Factory artifacts related to the ingestion layer.

Confirmed pipeline:

```text
PL_Ingest_ProductivityMonitor_DailyFile
```

Expected subfolders:

```text
adf/
├── pipelines/
└── datasets/
```

The actual exported ADF JSON files were not provided in the current documentation input, so this folder currently contains placeholders only.

## Pending Confirmation

- Pipeline export JSON.
- Dataset export JSON.
- Linked service export JSON.
- Self-hosted Integration Runtime name.
- Trigger configuration, if any.
