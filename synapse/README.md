# Synapse Artifacts

This folder contains SQL scripts for the Azure Synapse Analytics Serverless SQL serving layer.

Confirmed Synapse workspace:

```text
syn-productivitymonitor-dev
```

Confirmed database:

```text
productivity_monitor_db
```

Confirmed external data source:

```text
GoldLake
```

Confirmed credential:

```text
SynapseWorkspaceIdentity
```

SQL scripts are stored under:

```text
synapse/sql/
```
